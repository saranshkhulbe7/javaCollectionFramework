import java.util.*;
public class LearnTreeHashSet {
    public static void main(String args[])
    {
        // in sorted order
        TreeSet<Integer> tSet= new TreeSet<>();
        tSet.add(45);
        tSet.add(53);
        tSet.add(89);
        tSet.add(78);

        System.out.println(tSet);
    }
}
